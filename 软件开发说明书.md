# DOCX to Markdown 转换器 - 软件开发说明书

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [核心模块详解](#3-核心模块详解)
4. [关键技术实现](#4-关键技术实现)
5. [设计决策与取舍](#5-设计决策与取舍)
6. [易错点与解决方案](#6-易错点与解决方案)
7. [代码规范](#7-代码规范)

---

## 1. 项目概述

### 1.1 项目目标

开发一款通用的 DOCX 文档转 Markdown 格式工具，支持：
- 智能识别 Word 原生标题样式
- 保留 Word 自动编号
- 表格智能转换（简单→Markdown，复杂→HTML）
- 段落格式保留（首行缩进）
- 批量转换功能
- 图形化界面

### 1.2 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| 编程语言 | Python 3.8+ | 生态丰富，文档处理库成熟 |
| DOCX 解析 | python-docx | 官方维护，API 稳定 |
| GUI 框架 | Tkinter | Python 内置，无需额外安装 |
| 打包工具 | PyInstaller | 支持 Windows/macOS/Linux |
| 表格检测 | XML 属性分析 | 直接读取 OOXML 底层结构 |

### 1.3 项目结构

```
scripts/
├── enhanced_convert.py      # 核心转换引擎（CLI/GUI 共用）
├── gui_app_tkinter.py      # Tkinter GUI 界面
├── gui_app.spec            # PyInstaller 打包配置
├── requirements.txt         # Python 依赖
├── 使用手册.md             # 用户使用文档
├── 软件开发说明书.md       # 本文档
└── 打包.bat               # Windows 打包脚本
```

---

## 2. 技术架构

### 2.1 模块依赖图

```
┌─────────────────────────────────────────────────────┐
│                    GUI 层                           │
│              (gui_app_tkinter.py)                  │
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 文件选择    │  │ 批量管理    │  │ 结果预览    │   │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘   │
└─────────┼─────────────────┼─────────────────┼─────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────┐
│                 转换引擎层                           │
│              (enhanced_convert.py)                   │
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐│
│  │Numbering    │  │ Heading     │  │ Table       ││
│  │Extractor     │  │ Detector    │  │ Analyzer    ││
│  └─────────────┘  └─────────────┘  └─────────────┘│
│                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐│
│  │ Table       │  │HeaderFooter│  │ TextPost   ││
│  │ Converter   │  │ Handler     │  │ Processor  ││
│  └─────────────┘  └─────────────┘  └─────────────┘│
└─────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────┐
│                  库层                               │
│         (python-docx / Tkinter)                    │
└─────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
DOCX 文件
    │
    ▼
┌─────────────────┐
│ Document 加载    │ python-docx
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 解析 XML 结构     │ 遍历 body.children
└────────┬────────┘
         │
         ├──────────────────┬──────────────────┐
         ▼                  ▼                  ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│  段落处理        │ │  表格处理        │ │  编号提取        │
│ - 样式检测      │ │ - 复杂度分析    │ │ - numbering.xml │
│ - 编号前缀      │ │ - 格式转换      │ │ - abstractNum   │
│ - 首行缩进      │ │                 │ │                 │
└────────┬────────┘ └────────┬────────┘ └────────┬────────┘
         │                  │                  │
         └──────────────────┴──────────────────┘
                           │
                           ▼
                 ┌─────────────────┐
                 │  Markdown 输出   │
                 └────────┬────────┘
                          │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    ┌─────────┐      ┌─────────┐      ┌─────────┐
    │ 保存文件 │      │ GUI预览  │      │ 返回内容 │
    └─────────┘      └─────────┘      └─────────┘
```

---

## 3. 核心模块详解

### 3.1 NumberingExtractor - Word 编号提取器

**文件**: `enhanced_convert.py` (第 588-812 行)

**功能**: 从 Word 的 numbering.xml 中提取自动编号格式

**核心数据结构**:

```python
class NumberingExtractor:
    # 存储 abstractNum 定义
    # abstractNumId -> {ilvl -> {'start', 'format', 'lvlText'}}
    abstract_nums: Dict[str, Dict]
    
    # 存储 num 到 abstractNum 的映射
    # numId -> abstractNumId
    nums: Dict[str, str]
    
    # 计数器，用于生成连续编号
    # (numId, ilvl) -> current_count
    counters: Dict[Tuple[str, int], int]
```

**关键方法**:

| 方法 | 说明 | 返回值 |
|------|------|--------|
| `get_numbering_info(para)` | 获取段落的编号信息 | `(numId, ilvl)` 或 `None` |
| `get_number_text(numId, ilvl)` | 生成编号文本 | `"1.1 "` 或类似格式 |

**支持的编号格式**:

| format 值 | 示例输出 |
|-----------|----------|
| `decimal` | 1, 2, 3... |
| `lowerLetter` | a, b, c... |
| `upperLetter` | A, B, C... |
| `lowerRoman` | i, ii, iii... |
| `upperRoman` | I, II, III... |
| `chineseCountingThousand` | 一, 二, 三... |

**lvlText 占位符替换**:

```python
# lvlText = "%1.%2." 表示多级编号
# 替换 %1 -> 第一级计数, %2 -> 第二级计数
```

### 3.2 HeadingDetector - 标题检测器

**文件**: `enhanced_convert.py` (第 75-187 行)

**功能**: 识别 Word 中的标题段落

**检测策略**:

```python
# 策略1: Word 原生样式（高优先级）
if style_name.startswith('Heading'):
    return True, int(style_number)

# 策略2: 中文标题样式
if re.match(r'^标题\s*(\d+)$', style_name):
    return True, int(style_number)

# 策略3: 启发式小标题检测（低优先级，可关闭）
# 仅当启用"小标题检测"选项时生效
if self.enable_subheading and is_likely_subheading(text):
    return True, 4  # 默认降级为4级标题
```

**启发式小标题模式**:

| 模式 | 示例 | 说明 |
|------|------|------|
| `^第[一二三四五六七八九十]+[章节条款]` | 第一章、第一节 | 中文章节编号 |
| `^\d+[.．]\d+` | 1.1、1.2.3 | 数字层级编号 |
| `^[（\(【\[].*[）\)\]】\]]$` | （标题） | 带括号的短文本 |

**易错点**: 启发式检测过于激进可能产生误判，因此默认关闭。

### 3.3 TableAnalyzer / TableConverter - 表格处理

**文件**: `enhanced_convert.py` (第 250-528 行)

**功能**: 分析表格复杂度并转换为对应格式

#### 3.3.1 复杂度判断

```python
class TableComplexity(Enum):
    SIMPLE = "simple"      # 简单表格 → Markdown
    COMPLEX = "complex"     # 复杂表格 → HTML
```

**复杂表格特征**:

| 特征 | XML 属性 | python-docx 属性 |
|------|----------|-----------------|
| 横向合并（colspan） | `w:gridSpan` | `tc.grid_span` |
| 纵向合并（rowspan） | `w:vMerge` | `tc.find(qn('w:vMerge'))` |

**⚠️ 关键易错点**: `vMerge` 属性检测

```python
# ❌ 错误方式：python-docx 的 hasattr 总是返回 True
if hasattr(tc, 'vMerge') and tc.vMerge:

# ✅ 正确方式：直接检查 XML 元素
vmerge_elem = tc.find(qn('w:vMerge'))
if vmerge_elem is not None:
    # vMerge="restart" 表示合并起始
    # vMerge 存在但无值 表示继续
```

#### 3.3.2 表格转换流程

```
┌─────────────────┐
│ 分析每个单元格    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ grid_span > 1?  │──否──► 正常单元格
└────────┬────────┘
         │是
         ▼
┌─────────────────┐
│ 展开为多列       │     ┌─────────┐
│ colspan=2 → 输2次│────►│ 文本 │ │ (空)
└─────────────────┘     └─────────┘

┌─────────────────┐
│ vMerge 存在?    │──否──► 正常单元格
└────────┬────────┘
         │是
         ▼
┌─────────────────┐
│ vMerge="restart"│──是──► 起始单元格，rowspan=N
└────────┬────────┘
         │否
         ▼
┌─────────────────┐
│ 跳过此单元格     │     (纵向合并继续，前一行已占位)
└─────────────────┘
```

#### 3.3.3 Markdown 表格输出

```python
def to_markdown_table(table) -> str:
    # 1. 计算总列数（考虑 colspan）
    total_cols = _calculate_total_columns(table)
    
    # 2. 遍历每行，展开 colspan
    # [{text: "A", colspan: 2}] → ["A", ""]
    
    # 3. 处理纵向合并
    # 合并行的后续行输出空格占位
```

### 3.4 TextPostProcessor - 文本后处理器

**文件**: `enhanced_convert.py` (第 57-95 行)

**功能**: 清理和规范化转换后的文本

#### 3.4.1 中文间距修复

```python
def fix_chinese_spacing(text: str) -> str:
    # 移除中文与英文之间多余的空格
    # "中文 English" → "中文English" 或 "中文 English"
    
    # 移除中文之间多余的空格
    # "中 文" → "中文"
    
    # 添加中文与英文之间必要的空格
    # "中文English" → "中文 English"
```

**正则规则**:

| 规则 | 原文本 | 处理后 |
|------|--------|--------|
| `[CJK] [a-zA-Z]` | `中 English` | `中English` |
| `[a-zA-Z] [CJK]` | `Hello 中` | `Hello中` |
| `[CJK]([a-zA-Z])` | `中E` | `中 E` |
| `([a-zA-Z])[CJK]` | `e中` | `e 中` |

#### 3.4.2 空行清理

```python
def remove_empty_lines(text: str, max_consecutive: int = 1) -> str:
    # 将连续的空行压缩为最多 max_consecutive 个
    # 用于清理图片等元素留下的多余空行
    
    # 示例:
    # 输入: "text\n\n\n\ntext"  (4个空行)
    # 输出: "text\n\ntext"      (1个空行)
```

### 3.5 HeaderFooterHandler - 页眉页脚处理

**文件**: `enhanced_convert.py` (第 527-577 行)

**功能**: 提取并过滤页眉页脚内容

```python
def extract_header_footer_text(doc) -> Set[str]:
    # 遍历 doc.sections
    # 每个 section 有 header 和 footer
    # 提取所有文本，存入集合
    
def is_page_number(text: str) -> bool:
    # 检测是否为纯数字页码
    # 例如: "1", "2", "10", "100"
```

---

## 4. 关键技术实现

### 4.1 按文档顺序处理元素

**问题**: python-docx 的 `doc.paragraphs` 和 `doc.tables` 是分开存储的

**解决**: 直接遍历 `doc.element.body` 的子元素

```python
body = doc.element.body

for child in body:
    tag = child.tag.split('}')[1] if '}' in child.tag else child.tag
    
    if tag == 'p':      # 段落
        # 处理段落
    elif tag == 'tbl':   # 表格
        # 处理表格
```

**好处**: 保持文档原始顺序，表格位置正确

### 4.2 段落缓冲区机制

**目的**: 实现段落合并功能

```python
pending_paragraph_buffer = []

for para in paragraphs:
    if is_heading(para):
        # 输出缓冲区的段落
        output_pending()
        # 输出标题
    elif is_paragraph(para):
        # 加入缓冲区
        pending_paragraph_buffer.append(para.text)
```

### 4.3 GUI 多线程设计

**问题**: 转换可能耗时较长，GUI 需要保持响应

**解决**: 使用独立线程执行转换

```python
class ConversionThread(threading.Thread):
    def run(self):
        # 在后台线程执行转换
        content, metadata = converter.convert(...)
        
        # 通过回调通知主线程
        self.callback('finished', {'metadata': metadata, 'content': content})
```

**回调事件类型**:

| 事件 | 触发时机 | 用途 |
|------|----------|------|
| `progress` | 进度更新 | 更新进度条 |
| `status` | 状态文本更新 | 显示当前处理的文件 |
| `finished` | 转换完成 | 显示结果 |
| `error` | 转换失败 | 显示错误信息 |

### 4.4 批量转换实现

```python
class BatchConversionThread(threading.Thread):
    def run(self):
        for i, input_path in enumerate(file_list):
            # 计算进度
            base_progress = (i * 100) // total
            
            # 生成输出路径
            output_path = input_path.replace('.docx', '.md')
            
            # 执行单个转换
            converter.convert(input_path, output_path)
            
            # 保存结果
            results.append((input_path, output_path, success, metadata))
```

---

## 5. 设计决策与取舍

### 5.1 Tkinter vs PyQt5

| 考量 | Tkinter | PyQt5 |
|------|---------|-------|
| 安装 | Python 内置 | 需单独安装 |
| 打包体积 | 较小 | 较大 |
| 功能 | 基础够用 | 更丰富 |
| 兼容性 | 更好 | 依赖 Qt 库 |

**决策**: 采用 Tkinter，降低用户使用门槛

### 5.2 简单/复杂表格二分法

**方案A**: 全部转为 HTML
- 优点: 支持所有表格特性
- 缺点: 纯 Markdown 渲染器可能不支持

**方案B**: 全部转为 Markdown
- 优点: 兼容性最好
- 缺点: 无法处理合并单元格

**方案C**: 智能二分（当前采用）
- 简单表格 → Markdown
- 复杂表格 → HTML
- 优点: 平衡兼容性和功能性
- 缺点: 需要准确判断复杂度

### 5.3 启发式小标题检测

**问题**: 很多中文文档不使用 Word 原生标题样式

**方案**: 提供启发式检测作为补充

**风险**: 可能产生误判

**决策**: 
- 功能默认关闭
- 用户可手动启用
- 仅作为 4 级标题处理

### 5.4 输出路径策略

**决策**: 自动生成，不提供手动选择

**理由**:
- 简化用户操作
- 避免路径选择错误
- 与源文件同目录便于管理

---

## 6. 易错点与解决方案

### 6.1 python-docx 属性名与 XML 属性名不一致

| 功能 | XML 属性 | python-docx 属性 | 正确读取方式 |
|------|----------|------------------|-------------|
| 横向合并 | `w:gridSpan` | `tc.grid_span` | `tc.grid_span` |
| 纵向合并 | `w:vMerge` | 不可用 | `tc.find(qn('w:vMerge'))` |

**错误代码**:
```python
# ❌ 错误：gridSpan 不存在
if tc.gridSpan > 1:

# ❌ 错误：vMerge 总是存在
if hasattr(tc, 'vMerge') and tc.vMerge:
```

**正确代码**:
```python
# ✅ 正确：使用 grid_span
if tc.grid_span > 1:

# ✅ 正确：检查 XML 元素
vmerge_elem = tc.find(qn('w:vMerge'))
if vmerge_elem is not None:
```

### 6.2 表格与段落顺序错乱

**问题**: 分别遍历 `doc.paragraphs` 和 `doc.tables` 会丢失相对顺序

**错误做法**:
```python
# ❌ 错误：先处理所有段落，再处理所有表格
for para in doc.paragraphs:
    process(para)
for table in doc.tables:
    process(table)
# 表格会全部出现在文档末尾
```

**正确做法**:
```python
# ✅ 正确：按 body 顺序遍历
for child in doc.element.body:
    if child.tag.endswith('p}'):
        process_paragraph(child)
    elif child.tag.endswith('tbl}'):
        process_table(child)
```

### 6.3 纵向合并单元格被重复输出

**问题**: 纵向合并的"继续"单元格在 python-docx 的 `row.cells` 中仍然存在

**错误处理**:
```python
# ❌ 错误：直接遍历 row.cells
for cell in row.cells:
    output(cell.text)  # 合并继续单元格会被输出为空
```

**正确处理**:
```python
# ✅ 正确：检查 vMerge 属性，跳过继续单元格
for cell in row.cells:
    tc = cell._tc
    vmerge_elem = tc.find(qn('w:vMerge'))
    if vmerge_elem is not None and vmerge_elem.get(qn('w:val')) != 'restart':
        continue  # 跳过纵向合并继续单元格
    output(cell.text)
```

### 6.4 GUI 回调在关闭窗口后执行

**问题**: 用户关闭窗口后，后台线程仍可能调用回调

**解决**: 在回调中检查窗口状态

```python
def on_conversion_callback(self, event_type, data):
    try:
        self.root.winfo_exists()  # 检查窗口是否还存在
    except:
        return  # 窗口已关闭，直接返回
```

### 6.5 中文编码与 Tkinter

**问题**: Windows 中文环境下的编码问题

**解决**:
```python
# 1. 文件开头声明编码
# -*- coding: utf-8 -*-

# 2. 批处理文件使用 UTF-8
chcp 65001

# 3. Tkinter 文本使用 Unicode
self.label.config(text="中文")
```

### 6.6 PyInstaller 打包依赖缺失

**问题**: 运行时提示找不到模块

**解决**: 在 spec 文件中声明 hiddenimports

```python
a = Analysis(
    ['gui_app_tkinter.py'],
    hiddenimports=['PIL', 'PIL._imagingtk'],  # 声明隐式依赖
)
```

### 6.7 表格合并单元格处理（重点）

表格合并是 DOCX 转换中最复杂的部分，涉及 rowspan（纵向合并）和 colspan（横向合并）。

#### 6.7.1 基本概念

| 概念 | XML 属性 | 说明 |
|------|----------|------|
| 横向合并 | `w:gridSpan` | 一个单元格跨多列 |
| 纵向合并 | `w:vMerge` | 一个单元格跨多行 |

**vMerge 状态**:
- `vMerge="restart"`: 合并起始单元格，包含实际内容
- `vMerge` 存在但无值（或为"continue"）: 合并继续单元格，内容为空

#### 6.7.2 python-docx 的坑

**问题1**: `cell.text` 对 vMerge 单元格返回错误内容

```python
# ❌ 错误：vMerge=continue 的单元格 text 为空，但 cell.text 可能返回前一个单元格的内容
for row in table.rows:
    for cell in row.cells:
        print(cell.text)  # 可能返回错误内容

# ✅ 正确：直接解析 word/document.xml 获取单元格文本
# 参见 _extract_table_cells_xml() 方法
```

**问题2**: `tc.grid_span` 可用，但 `tc.vMerge` 不可用

```python
# ✅ 正确：gridSpan 有对应的 python-docx 属性
if tc.grid_span > 1:
    colspan = tc.grid_span

# ✅ 正确：vMerge 必须通过 XML 元素检查
vmerge_elem = tc.find(qn('w:vMerge'))
if vmerge_elem is not None:
    vm = vmerge_elem.get(qn('w:val'))  # "restart" 或 None
```

#### 6.7.3 纵向合并（rowspan）计算

**核心算法**: `_calc_rowspan()` 方法

```python
def _calc_rowspan(cell_data: List[List[Dict]], ri: int, ci: int, n_rows: int,
                  row_grid_starts: List[List[int]]) -> int:
    """
    计算纵向合并单元格跨越的行数
    
    关键点：
    1. 只有 vMerge="restart" 的单元格需要计算 rowspan
    2. 需要检查后续所有行中，对应 grid 位置是否有继续单元格
    3. 需要考虑 colspan>1 的情况（检查所有被占据的 grid 位置）
    """
```

**grid_starts 的作用**: 记录每行每个单元格占据的 grid 起始位置

```python
# 例如：某行有 colspan=2 的单元格和 colspan=1 的单元格
# grid_starts = [[0, 2]]  # 第一个单元格从 grid 0 开始，第二个从 grid 2 开始
```

**易错点1**: 只检查单元格索引，忽略 gridSpan

```python
# ❌ 错误：只检查了 ci 位置
for nr in range(ri + 1, n_rows):
    if cell_data[nr][ci]['vm'] == 'continue':  # 错误！
        ...

# ✅ 正确：检查单元格占据的所有 grid 位置
gc_start = row_grid_starts[ri][ci]  # 当前单元格的 grid 起始位置
gs = cell['gs']  # gridSpan
for offset in range(gs):
    gc = gc_start + offset
    # 找到 nr 行中占据 grid 位置 gc 的单元格
    ...
```

**易错点2**: 两个连续的 vMerge="restart" 被错误合并

```python
# 场景：第二行和第三行都有 vMerge="restart"，文本相同
# 预期：这是两个独立的合并，不是延续关系

# ❌ 错误：只检查文本相同就延续
if next_tc['vm'] is None and next_tc['text'] == cell['text']:
    found_continue = True  # 错误！

# ✅ 正确：vMerge="restart" 表示新合并开始，应停止当前合并
if next_tc['vm'] == 'continue':
    found_continue = True
elif next_tc['vm'] is None and next_tc['text'] == cell['text']:
    found_continue = True
else:
    found_continue = False
```

#### 6.7.4 横向合并（colspan）与纵向合并的交互

**场景**: 单元格同时有 colspan>1 和 vMerge="restart"

```python
# 某单元格有 gridSpan=2 且 vMerge="restart"
# 这意味着：
# 1. 这个单元格占据 2 列
# 2. 这个单元格纵向跨 2+ 行

# row_grid_starts 必须正确记录：
# [grid_start, grid_start + gridSpan]
```

**易错点**: colspan>1 时 row_grid_starts 计算错误

```python
# 构建 row_grid_starts 时：
for ci, cell in enumerate(cells):
    if ci == 0:
        grid_starts.append(0)
    else:
        # 应该基于前一个单元格的 gridSpan 计算
        prev_gs = cells[ci-1].get('gridSpan', 1)
        prev_gs = prev_gs if prev_gs > 0 else 1
        grid_starts.append(grid_starts[-1] + prev_gs)
```

#### 6.7.5 完整转换流程

```
┌─────────────────────────────────────────┐
│ 1. 解析表格 XML                         │
│    _extract_table_cells_xml()           │
│    - 遍历每个单元格                      │
│    - 记录 text, gridSpan, vMerge        │
│    - 构建 row_grid_starts               │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│ 2. 分析复杂度                           │
│    _analyze_table_complexity()          │
│    - 检查是否有 gridSpan>1              │
│    - 检查是否有 vMerge                  │
│    - 简单表格 → Markdown                │
│    - 复杂表格 → HTML                    │
└────────────────┬────────────────────────┘
                 │
       ┌─────────┴─────────┐
       ▼                   ▼
┌─────────────┐     ┌─────────────┐
│ Markdown    │     │ HTML        │
│ to_markdown │     │ to_html_table│
│ _table()    │     │ ()          │
└─────────────┘     └─────────────┘
```

#### 6.7.6 HTML 表格输出要点

```python
def to_html_table(table_data: List[List[Dict]]) -> str:
    """转换为 HTML 表格
    
    关键点：
    1. 使用 <table> 标签
    2. 正确设置 colspan 和 rowspan 属性
    3. 跳过 vMerge=continue 的单元格（已在前一行输出）
    """
    # 输出顺序：先处理起始单元格，再处理继续单元格
    # rowspan/colspan 正确设置
```

---

## 7. 代码规范

### 7.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 类名 | PascalCase | `TableAnalyzer` |
| 函数名 | snake_case | `to_markdown_table` |
| 私有方法 | `_` 前缀 | `_calculate_total_columns` |
| 常量 | UPPER_SNAKE | `MAX_HEADING_LEVEL` |
| 变量 | snake_case | `pending_buffer` |

### 7.2 类型注解

```python
from typing import Optional, Dict, List, Set, Tuple

def convert(
    doc_path: str, 
    output_path: Optional[str] = None
) -> Tuple[str, Dict]:
    """转换 DOCX 文件"""
    ...
```

### 7.3 文档字符串

```python
def to_markdown_table(table) -> str:
    """
    将表格转换为 Markdown 格式
    
    正确处理:
    - colspan (gridSpan > 1): 输出多个列，空的用空格填充
    - 纵向合并单元格: 输出文本，空的后续行用空格填充
    
    Args:
        table: python-docx Table 对象
        
    Returns:
        Markdown 格式的表格字符串
    """
```

### 7.4 错误处理模式

```python
try:
    content, metadata = converter.convert(input_path, output_path)
except FileNotFoundError:
    return {"error": f"文件不存在: {input_path}"}
except Exception as e:
    return {"error": f"转换失败: {str(e)}"}
```

---

## 附录

### A. 相关资源

- python-docx 文档: https://python-docx.readthedocs.io/
- Tkinter 教程: https://docs.python.org/3/library/tkinter.html
- PyInstaller 文档: https://pyinstaller.org/

### B. 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.1.0 | 2026-03-26 | 修复复杂表格转换：纵向合并单元格处理、gridSpan 与 vMerge 交互问题 |
| 1.0.0 | 2024-03-21 | 正式版发布 |

### C. 已知限制

1. 不支持 .doc 格式（仅支持 .docx）
2. 不支持宏和 ActiveX 控件
3. 不支持修订模式（Track Changes）
4. 图片会被忽略（不输出到 Markdown）

---

*本说明书最后更新: 2026-03-26*

&nbsp;

---

**作者：章非**
